# Create GPX Files

Python script which uses Selenium to batch create GPX files from Google Maps URLs.

Used to create data for the Leaflet map in [Wheelie Babes Bike Blog](https://wheelie-babes.bsik.net).
